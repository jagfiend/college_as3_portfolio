package classes.game // this class is used to hold code to draw the circle graphics used by the projectile and missile classes...{	// import libraries...	import flash.display.MovieClip;	import flash.events.Event; 	import flash.filters.BitmapFilterQuality;	import flash.filters.BlurFilter;	import flash.filters.GlowFilter;	// class code...	public class DrawMovingObject extends MovieClip	{		// class variables...		protected var _game:Game;		// circle variables..		protected var circle:MovieClip;		private var circleColour:uint;		private var circleSize:int;		// blur variables...		protected var blur:BlurFilter = new BlurFilter();		private var blurLevel:int;		// glow variables...		protected var glow:GlowFilter = new GlowFilter();		private var glowColour:uint;		private var glowBlurLevel:int;		// class functions...		public function DrawMovingObject(game:Game, size:int, colour:uint, blurAmount:int, glowColour:uint, glowBlur:int) 		{			circleSize = size;			circleColour = colour;			this.blurLevel = blurAmount;			this.glowColour = glowColour;			this.glowBlurLevel = glowBlur;			// constructor code			_game = game;			// code for instantiating circle movieclip...			circle = new MovieClip();			addChild(circle);			// code to define circle blur...			blur.blurX = blurLevel;			blur.blurY = blurLevel;			blur.quality = BitmapFilterQuality.HIGH;			// code to define glow around circle...			glow.color = glowColour; 			glow.alpha = 1; 			glow.blurX = glowBlurLevel; 			glow.blurY = glowBlurLevel; 			glow.quality = BitmapFilterQuality.HIGH;			drawCircle();		}				private function drawCircle():void		{			circle.graphics.clear(); // removes previous graphic from previous frame...			circle.filters = []; // clears any miscellaneous lurking filters...			circle.graphics.beginFill(circleColour,1); // begins a circle fill......			circle.graphics.drawCircle(0,0,circleSize); // defines cirle size to fill...			circle.filters = [blur,glow]; // adds a tasteful filter or two...		} // end of functions	} // end of class} // end of package